// map use to loop through all elements and get a new modified array.
var fruits = ["apple", "mango", "cherry", "grapes"]
var upperCaseNames = fruits.map(fruit => fruit.toUpperCase());
console.log("upperCase", upperCaseNames);
